[build]
  command = "npm run build"
  publish = "dist"

# Frame sequence is content-stable and versioned by filename: cache hard.
[[headers]]
  for = "/frames/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
